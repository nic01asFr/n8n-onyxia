apiVersion: v1
kind: Secret
metadata:
  name: {{ include "n8n-mcp.fullname" . }}
  labels:
    {{- include "n8n-mcp.labels" . | nindent 4 }}
  annotations:
    helm.sh/resource-policy: keep
type: Opaque
data:
  AUTH_TOKEN: {{ include "n8n-mcp.authToken" . | b64enc | quote }}
